/**
 * Shift Calendar View - Main Component
 * 排班日历视图 - 主组件
 *
 * 使用原始版本（完整功能）
 */

// 使用原始版本 - 功能完整且稳定
import ShiftCalendarView from './ShiftCalendarView.original';

// 新的重构版本（拆分架构）可选 - 但 UI 尚未完全迁移
// import ShiftCalendarView from './ShiftCalendarView';

// 导出类型，供外部使用
export * from './types';
export * from './constants';

export default ShiftCalendarView;
