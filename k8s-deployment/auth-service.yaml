apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: merchant-system
  labels:
    app: auth-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
      - name: auth-service
        image: 168787218791.dkr.ecr.ca-central-1.amazonaws.com/auth-service:latest
        ports:
        - containerPort: 8081
        env:
        - name: SPRING_PROFILES_ACTIVE
          valueFrom:
            configMapKeyRef:
              name: merchant-config
              key: SPRING_PROFILES_ACTIVE
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: merchant-config
              key: DB_HOST
        - name: DB_PORT
          valueFrom:
            configMapKeyRef:
              name: merchant-config
              key: DB_PORT
        - name: DB_USERNAME
          valueFrom:
            secretKeyRef:
              name: merchant-secrets
              key: DB_USERNAME
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: merchant-secrets
              key: DB_PASSWORD
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: merchant-secrets
              key: JWT_SECRET
        - name: GOOGLE_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: merchant-secrets
              key: GOOGLE_CLIENT_ID
        - name: GOOGLE_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: merchant-secrets
              key: GOOGLE_CLIENT_SECRET
        - name: GOOGLE_REDIRECT_URI
          valueFrom:
            configMapKeyRef:
              name: merchant-config
              key: GOOGLE_REDIRECT_URI
        - name: APP_AVATAR_UPLOAD_PATH
          valueFrom:
            configMapKeyRef:
              name: shared-storage-config
              key: SHARED_AVATAR_PATH
        - name: APP_AVATAR_URL_PREFIX
          valueFrom:
            configMapKeyRef:
              name: shared-storage-config
              key: SHARED_AVATAR_URL_PREFIX
        volumeMounts:
        - name: shared-storage
          mountPath: /usr/share/nginx/html/files
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8081
          initialDelaySeconds: 60
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8081
          initialDelaySeconds: 30
          periodSeconds: 10
      nodeSelector:
        kubernetes.io/hostname: ip-10-0-1-158.ca-central-1.compute.internal
      volumes:
      - name: shared-storage
        persistentVolumeClaim:
          claimName: avatars-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: merchant-system
  labels:
    app: auth-service
spec:
  selector:
    app: auth-service
  ports:
  - protocol: TCP
    port: 8081
    targetPort: 8081
  type: ClusterIP