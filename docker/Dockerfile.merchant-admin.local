# 使用本地构建结果的Dockerfile
FROM nginx:alpine

# 复制本地构建结果
COPY merchant-admin/build /usr/share/nginx/html

# 复制nginx配置
COPY docker/nginx.conf /etc/nginx/nginx.conf

# 暴露端口
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]