<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{traceId:-NO_TRACE}] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 文件输出 - 自动日志轮换 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>../logs/analytics-service.log</file>
        <append>true</append>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>../logs/analytics-service.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{traceId:-NO_TRACE}] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 应用程序日志 - 只输出到文件，避免Maven重定向导致的重复日志 -->
    <logger name="com.merchant.server.analyticsservice" level="INFO" additivity="false">
        <appender-ref ref="FILE"/>
    </logger>

    <!-- 减少MyBatis和Spring框架的日志输出 -->
    <logger name="com.merchant.server.analyticsservice.mapper" level="WARN"/>
    <logger name="org.apache.ibatis" level="WARN"/>
    <logger name="java.sql" level="WARN"/>
    <logger name="org.mybatis" level="ERROR"/>
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.springframework.cloud" level="WARN"/>
    <logger name="org.springframework.context.support.PostProcessorRegistrationDelegate" level="ERROR"/>
    <logger name="org.springframework.cloud.loadbalancer" level="ERROR"/>
    <logger name="com.netflix" level="WARN"/>

    <!-- 根日志级别 - 只输出到文件 -->
    <root level="INFO">
        <appender-ref ref="FILE"/>
    </root>
</configuration>