<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.merchant.server.authservice.mapper.UserRoleMapper">

    <!-- Result Map -->
    <resultMap id="BaseResultMap" type="com.merchant.server.authservice.entity.UserRole">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="role_id" property="roleId"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="effective_from" property="effectiveFrom"/>
        <result column="effective_to" property="effectiveTo"/>
        <result column="is_primary" property="isPrimary"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="created_by" property="createdBy"/>
    </resultMap>

    <!-- Base Column List -->
    <sql id="Base_Column_List">
        id, user_id, role_id, tenant_id, effective_from, effective_to, is_primary, created_at, updated_at, created_by
    </sql>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_roles
        WHERE id = #{id}
    </select>

    <!-- 根据用户ID查询所有角色关联 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_roles
        WHERE user_id = #{userId}
        ORDER BY is_primary DESC, id
    </select>

    <!-- 根据用户ID和租户ID查询角色关联 -->
    <select id="selectByUserIdAndTenantId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_roles
        WHERE user_id = #{userId}
          AND (tenant_id = #{tenantId} OR tenant_id IS NULL)
        ORDER BY is_primary DESC, id
    </select>

    <!-- 根据角色ID查询所有用户关联 -->
    <select id="selectByRoleId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_roles
        WHERE role_id = #{roleId}
        ORDER BY id
    </select>

    <!-- 根据用户ID和角色ID查询 -->
    <select id="selectByUserIdAndRoleId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_roles
        WHERE user_id = #{userId}
          AND role_id = #{roleId}
        LIMIT 1
    </select>

    <!-- 查询用户的主角色 -->
    <select id="selectPrimaryByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_roles
        WHERE user_id = #{userId}
          <if test="tenantId != null">
          AND (tenant_id = #{tenantId} OR tenant_id IS NULL)
          </if>
          AND is_primary = TRUE
        ORDER BY id DESC
        LIMIT 1
    </select>

    <!-- 查询用户的有效角色（在生效日期范围内） -->
    <select id="selectEffectiveByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_roles
        WHERE user_id = #{userId}
          <if test="tenantId != null">
          AND (tenant_id = #{tenantId} OR tenant_id IS NULL)
          </if>
          AND (effective_from IS NULL OR effective_from &lt;= CURRENT_DATE)
          AND (effective_to IS NULL OR effective_to &gt;= CURRENT_DATE)
        ORDER BY is_primary DESC, id
    </select>

    <!-- 插入用户角色关联 -->
    <insert id="insert" parameterType="com.merchant.server.authservice.entity.UserRole" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_roles (
            user_id, role_id, tenant_id, effective_from, effective_to, is_primary, created_by
        ) VALUES (
            #{userId}, #{roleId}, #{tenantId}, #{effectiveFrom}, #{effectiveTo}, #{isPrimary}, #{createdBy}
        )
    </insert>

    <!-- 更新用户角色关联 -->
    <update id="update" parameterType="com.merchant.server.authservice.entity.UserRole">
        UPDATE user_roles
        SET tenant_id = #{tenantId},
            effective_from = #{effectiveFrom},
            effective_to = #{effectiveTo},
            is_primary = #{isPrimary}
        WHERE id = #{id}
    </update>

    <!-- 删除用户角色关联 -->
    <delete id="deleteById">
        DELETE FROM user_roles WHERE id = #{id}
    </delete>

    <!-- 根据用户ID删除所有关联 -->
    <delete id="deleteByUserId">
        DELETE FROM user_roles WHERE user_id = #{userId}
    </delete>

    <!-- 根据角色ID删除所有关联 -->
    <delete id="deleteByRoleId">
        DELETE FROM user_roles WHERE role_id = #{roleId}
    </delete>

    <!-- 批量插入用户角色关联 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO user_roles (
            user_id, role_id, tenant_id, effective_from, effective_to, is_primary, created_by
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.userId}, #{item.roleId}, #{item.tenantId},
                #{item.effectiveFrom}, #{item.effectiveTo}, #{item.isPrimary}, #{item.createdBy}
            )
        </foreach>
    </insert>

    <!-- 取消用户的所有主角色标记 -->
    <update id="clearPrimaryByUserId">
        UPDATE user_roles
        SET is_primary = FALSE
        WHERE user_id = #{userId}
          <if test="tenantId != null">
          AND (tenant_id = #{tenantId} OR tenant_id IS NULL)
          </if>
    </update>

    <!-- 检查用户是否拥有指定角色 -->
    <select id="checkUserHasRole" resultType="int">
        SELECT COUNT(*)
        FROM user_roles
        WHERE user_id = #{userId}
          AND role_id = #{roleId}
          AND (effective_from IS NULL OR effective_from &lt;= CURRENT_DATE)
          AND (effective_to IS NULL OR effective_to &gt;= CURRENT_DATE)
    </select>

    <!-- 获取用户的角色ID列表 -->
    <select id="selectRoleIdsByUserId" resultType="long">
        SELECT role_id
        FROM user_roles
        WHERE user_id = #{userId}
          <if test="tenantId != null">
          AND (tenant_id = #{tenantId} OR tenant_id IS NULL)
          </if>
          AND (effective_from IS NULL OR effective_from &lt;= CURRENT_DATE)
          AND (effective_to IS NULL OR effective_to &gt;= CURRENT_DATE)
        ORDER BY is_primary DESC, role_id
    </select>

</mapper>
