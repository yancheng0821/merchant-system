<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{traceId:-NO_TRACE}] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 文件输出 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>../logs/business-service.log</file>
        <append>true</append> <!-- 明确设置为追加模式，服务重启时不覆盖日志 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>../logs/business-service.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
            <cleanHistoryOnStart>false</cleanHistoryOnStart> <!-- 启动时不清理历史日志 -->
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{traceId:-NO_TRACE}] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 设置MyBatis相关的日志级别为WARN，减少SQL日志输出 -->
    <logger name="com.merchant.server.businessservice.mapper" level="WARN"/>
    <logger name="org.apache.ibatis" level="WARN"/>
    <logger name="java.sql" level="WARN"/>
    <logger name="org.mybatis" level="ERROR"/>
    <logger name="org.mybatis.spring.mapper.ClassPathMapperScanner" level="ERROR"/>

    <!-- 设置应用程序的日志级别 -->
    <logger name="com.merchant.server.businessservice" level="INFO"/>

    <!-- Feign日志 -->
    <logger name="com.merchant.server.businessservice.client" level="DEBUG"/>

    <!-- 设置Spring相关日志级别 -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.springframework.context.support.PostProcessorRegistrationDelegate" level="ERROR"/>
    <logger name="org.springframework.cloud" level="WARN"/>
    <logger name="org.springframework.cloud.loadbalancer.cache.LoadBalancerCacheAutoConfiguration" level="ERROR"/>
    <logger name="org.apache.catalina" level="WARN"/>
    <logger name="org.apache.coyote" level="WARN"/>
    <logger name="org.apache.tomcat" level="WARN"/>

    <!-- Netflix Eureka -->
    <logger name="com.netflix" level="WARN"/>
    <logger name="com.netflix.discovery" level="WARN"/>
    
    <!-- 根日志级别 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>